# 新增功能文件描述文档

> 基于项目：Health-Life-HarmonyOS-Next  
> 新增日期：2026-05-13  
> 功能作者：watersail777

---

## 一、新增功能概述

本项目在原「健康生活」应用基础上新增了两项功能：

| 功能 | 定位 | 核心价值 |
|------|------|----------|
| **功能一：每日健康知识推送** | 首页信息卡片 | 增加用户停留动力，知识科普引导习惯养成 |
| **功能二：打卡统计图表** | 数据可视化 | 量化打卡数据，帮助用户直观了解习惯趋势 |
| **功能三：成就分享海报** | 社交激励 | 全部任务完成后弹出海报，可视化今日成就 |
| **功能四：自定义任务创建** | 个性扩展 | 突破7项预设，用户自由创建个性化健康任务 |

四项功能均遵循最小侵入原则，不破坏任何原有逻辑，分别新增/修改 3、6、5、10 个文件。

---

## 二、功能一：每日健康知识推送

### 2.1 功能定位

在首页新增「每日健康知识推送」卡片，用户每次打开应用都可以看到一条有科学依据的健康冷知识。支持展开/收起查看详情，不打断原有打卡流程。

### 2.2 解决的问题

| 问题 | 改进 |
|------|------|
| 原应用仅支持「任务打卡」，用户打开后缺乏停留动力 | 新增知识卡片增加信息获取乐趣 |
| 首页顶部进度区利用率低 | 在进度区和任务列表之间加入知识卡片，丰富页面层次 |
| 健康习惯养成缺乏知识引导 | 30条科学健康知识帮助用户理解"为什么要做这些任务" |

### 2.3 使用场景

- 用户每天早上打开应用打卡时，随机看到一条健康冷知识
- 看到感兴趣的标题可以点击展开阅读全文
- 读完可收起，不影响查看下方的任务列表

---

## 三、功能一 新增 / 修改文件清单

| 文件路径 | 变更类型 | 说明 |
|----------|:--:|------|
| `entry/src/main/ets/model/KnowledgeData.ets` | **新增** | 健康知识数据模型，包含 30 条结构化知识和按日期哈希的获取函数 |
| `entry/src/main/ets/view/KnowledgeCard.ets` | **新增** | 可展开/收起的知识卡片 UI 组件 |
| `entry/src/main/ets/view/HomeComponent.ets` | **修改** | 导入 KnowledgeCard 并集成到首页布局中（+3 行） |

### 变更详情

#### KnowledgeData.ets（新增，~250 行）

```
├── export interface KnowledgeItem      # 知识条目的数据结构定义
├── export const KNOWLEDGE_LIST         # 30条健康冷知识数组（5大类）
├── export function getDailyKnowledge() # 按日期哈希获取每日知识（同天同条）
└── export function getRandomKnowledge()# 完全随机获取（备用）
```

30 条知识覆盖 **5 个分类**：人体怪象（12条）、饮食健康（7条）、运动科学（5条）、睡眠心理（5条）、日常误区（2条）

#### KnowledgeCard.ets（新增，~110 行）

```
@Component
export default struct KnowledgeCard {
  @Prop knowledge: KnowledgeItem    // 接收父组件传入的知识数据
  @State isExpanded: boolean        // 控制展开/收起状态

  build() {
    Column() {
      Row() {                       // 头部：💡图标 + 标题 + 展开箭头
      图标 / 标题（单行省略） / ▼箭头
      }
      if (isExpanded) {             // 展开后才显示正文和分类
        Divider()
        正文内容（多行可读）
        分类标签（蓝色圆角）
      }
    }
    .onClick() → 切换 isExpanded
  }
}
```

#### HomeComponent.ets（修改，+3 行）

```diff
+ import KnowledgeCard from './KnowledgeCard';
+ import { KnowledgeItem, getDailyKnowledge } from '../model/KnowledgeData';

  @Component
  export default struct HomeIndex {
+   @State dailyKnowledge: KnowledgeItem = getDailyKnowledge();
    ...

    build() {
      HomeTopView({ homeStore: $homeStore })
+     KnowledgeCard({ knowledge: this.dailyKnowledge })  // 新增这一行
      Text($r('app.string.task_list')).titleTextStyle()
      ...
    }
  }
```

---

## 四、功能一 核心代码解析

### 4.1 每日知识获取算法（KnowledgeData.ets）

```typescript
export function getDailyKnowledge(date?: Date): KnowledgeItem {
  const targetDate = date || new Date();
  // 将年月日组合成一个整数值作为种子
  const seed = targetDate.getFullYear() * 10000
             + (targetDate.getMonth() + 1) * 100
             + targetDate.getDate();
  // Knuth 乘法哈希 → 取模得到数组索引
  const index = (seed * 2654435761) % KNOWLEDGE_LIST.length >>> 0;
  return KNOWLEDGE_LIST[index];
}
```

**设计要点**：
- 使用日期作为随机种子，保证**同一天返回同一条**，不会每次打开都变
- Knuth 乘法哈希（黄金比例 2654435761）保证相邻日期得到离散分布的知识条目
- 支持传入 Date 参数，方便测试

### 4.2 卡片展开/收起动画（KnowledgeCard.ets）

```typescript
@State isExpanded: boolean = false;

.onClick(() => {
  this.isExpanded = !this.isExpanded;  // 切换状态，触发 UI 自动重渲染
})
```

**设计要点**：
- ArkTS 声明式 UI：`isExpanded` 变化后，框架自动增删 `if (this.isExpanded)` 分支内的 DOM 节点
- 收起时标题限制单行 + 省略号（`maxLines(1) + TextOverflow.Ellipsis`），展开时允许最多 10 行
- 整个卡片区域可点击，交互热区大

### 4.3 集成方式（HomeComponent.ets）

**设计要点**：
- 最小侵入性原则：只在原有 `Column` 中新增 1 行组件调用，零风险
- 使用 `@State` 而非 `@Link`：知识数据只读，不需要父子双向同步
- 数据通过 `@Prop` 传入子组件，保持单向数据流

---

## 五、功能二：打卡统计图表

### 5.1 功能定位

新增第 4 个底部 Tab「统计」，提供打卡数据的可视化分析，包括 Canvas 柱状图展示近 7 天完成率趋势和 4 项汇总统计指标（累计打卡天数、当前连续天数、历史最佳、已获成就数）。

### 5.2 解决的问题

| 问题 | 改进 |
|------|------|
| 原应用只有打卡功能，用户无法了解自己的长期习惯趋势 | 柱状图直观展示每周打卡波动 |
| 缺少数据反馈激励 | 连续打卡天数等指标提供量化成就反馈 |
| 3 个 Tab 内容较简单 | 新增统计 Tab 丰富了应用的信息密度 |

### 5.3 使用场景

- 用户打卡一段时间后想了解自己的习惯养成情况
- 通过柱状图发现哪些天打卡率较低，针对性调整
- 连续天数指标激励用户保持打卡习惯

### 5.4 新增 / 修改文件清单

| 文件路径 | 变更类型 | 说明 |
|----------|:--:|------|
| `viewmodel/StatsViewModel.ets` | **新增** | 数据逻辑层：查询 DayInfo/GlobalInfo，计算连续天数、累计打卡等统计指标 |
| `view/ChartComponent.ets` | **新增** | Canvas 柱状图组件：7 天完成率趋势图，今日蓝色高亮 |
| `pages/StatsPage.ets` | **新增** | 统计页面：Loading 状态 + 4 指标卡片 + 图表 + 每日详情列表 |
| `model/NavItemModel.ets` | **修改** | TabId 枚举新增 STATS，NavList 数组新增统计导航项（第 3 位） |
| `pages/MainPage.ets` | **修改** | 导入 StatsIndex，在 Achievement 和 Mine 之间插入第 4 个 TabContent |
| `resources/base/element/string.json` | **修改** | 新增 tab_stats 字符串资源 |

### 5.5 核心代码解析

**StatsViewModel 数据计算（StatsViewModel.ets）**：

- `queryWeekStats()`：生成近 7 天日期列表，批量查询 DayInfo + GlobalInfo，返回组装好的 `StatsData`
- `calculateCurrentStreak()`：从今天往前遍历 DayStats 数组，百分百完成才累计连续天数，今天未完成不影响（继续往前数）
- `calculateBestStreak()`：解析 GlobalInfo 的 achievements 字段（如 "3,7,30"），取最大值作为历史最佳

**Canvas 柱状图渲染（ChartComponent.ets）**：

- 使用 ArkTS 标准画布模式：`RenderingContextSettings` + `CanvasRenderingContext2D`
- `drawChart()`：绘制参考线（0%/50%/100%）→ 遍历 DayStats 画 7 根柱子 → 柱顶百分比标签 → 柱底星期标签
- 今日柱子用 `#2196F3` 蓝色高亮，底部加圆点标记
- 所有变量显式类型注解，使用 `RefLine` 接口替代裸对象

**统计页面结构（StatsPage.ets）**：

- `aboutToAppear()` 和 `onPageShow()` 均触发 `loadData()`
- `@State statsData` 驱动 UI，加载完成后展示 4 个统计卡片 + 柱状图 + 每日详情行
- 每日详情行包含：日期、进度条（Stack 叠加）、百分比、状态图标（✅/🔄/⬜）

---

## 六、功能三：成就分享海报

### 6.1 功能定位

每日全部任务完成后自动弹出「成就分享海报」，以可视化方式展示今日打卡成果，包括日期、完成百分比、任务清单、连续打卡天数和已获成就勋章。海报底部提供关闭和分享按钮。

### 6.2 解决的问题

| 问题 | 改进 |
|------|------|
| 完成全部任务后缺乏仪式感 | 海报弹窗提供"今日达成"的可视化成就感反馈 |
| 打卡成就无法对外展示 | 海报设计美观，可用作社交分享素材 |
| 成就数据分散在各个页面 | 海报整合日期/进度/任务/成就/天数，一览无余 |

### 6.3 使用场景

- 用户每天完成全部打卡任务后，自动弹出海报
- 查看当日完成情况，截图分享给朋友
- 海报中连续打卡天数激励用户保持习惯

### 6.4 新增 / 修改文件清单

| 文件路径 | 变更类型 | 说明 |
|----------|:--:|------|
| `view/dialog/SharePosterDialog.ets` | **新增** | 海报弹窗组件：渐变标题栏、100%大字、任务清单、连续天数徽章、成就勋章行 |
| `view/dialog/CustomDialogView.ets` | **修改** | 导入 SharePosterDialog，注册 posterDialog 控制器和 SHOW_SHARE_POSTER_DIALOG 监听 |
| `view/HomeComponent.ets` | **修改** | 在 taskClock 回调中检测全部任务完成状态，触发海报弹窗 |
| `common/utils/BroadCast.ets` | **修改** | 新增 SHOW_SHARE_POSTER_DIALOG 枚举值，在 on/off/emit 中添加对应 case |
| `viewmodel/BroadCastCallBackInfo.ets` | **修改** | 新增 showSharePosterDialog 回调字段 |

### 6.5 核心代码解析

**触发逻辑（HomeComponent.ets）**：

```typescript
onConfirm(task: TaskInfo) {
  this.homeStore.taskClock(task).then((res: AchievementInfo) => {
    // 成就弹窗（里程碑触发）
    if (res.showAchievement) { ... }
    // 海报弹窗（全部完成触发）：检查当日finTaskNum >= targetTaskNum
    const dayInfo = this.homeStore.selectedDayInfo?.dayInfo;
    if (dayInfo && dayInfo.targetTaskNum > 0 &&
        dayInfo.finTaskNum >= dayInfo.targetTaskNum) {
      this.broadCast.emit(BroadCastType.SHOW_SHARE_POSTER_DIALOG);
    }
  })
}
```

**海报UI结构（SharePosterDialog.ets）**：

```
@CustomDialog
  ├── 标题栏："打卡成就" + 关闭按钮
  ├── SharePosterContent（海报主体）
  │     ├── 渐变装饰条 + "今日成就" 标题
  │     ├── 日期显示
  │     ├── 100% 大字
  │     ├── 全部任务已完成 🎉 文字
  │     ├── Divider 分隔线
  │     ├── 今日任务打卡清单（✅/⬜）
  │     ├── 🔥 连续打卡X天（蓝色渐变徽章）
  │     ├── 🏅 已获成就勋章行
  │     └── "健康生活 · Healthy Life" 品牌尾
  └── 底部按钮："关闭" + "分享成就✨"
```

---

## 七、功能四：自定义任务创建

### 7.1 功能定位

突破原应用 7 项预设任务的限制，用户可自由创建个性化健康任务（最多 5 个）。自定义任务 ID 从 8 开始，定义存储在 Preferences，打卡数据沿用现有 taskInfo 数据库表。支持创建、编辑、打卡、删除全流程。

### 7.2 解决的问题

| 问题 | 改进 |
|------|------|
| 原应用仅支持 7 项固定任务 | 用户可创建瑜伽、冥想、阅读等任意健康任务 |
| 无法满足个性化习惯需求 | 自定义名称、目标值、单位、步进、图标 |
| 硬编码 TASK_NUM=7 限制扩展 | DatabaseModel/HomeViewModel 全部改为动态数组长度 |

### 7.3 使用场景

- 用户想养成瑜伽习惯 → 创建「瑜伽」任务，目标 30 分钟，每次 +5
- 用户想记录阅读 → 创建「阅读」任务，目标 20 页，每次 +5
- 用户想练习冥想 → 创建「冥想」任务，目标 10 分钟，每次 +5

### 7.4 新增 / 修改文件清单

| 文件路径 | 变更类型 | 说明 |
|----------|:--:|------|
| `model/CustomTaskStore.ets` | **新增** | Preferences 存储自定义任务定义，同步缓存查表 |
| `pages/CustomTaskCreatePage.ets` | **新增** | 表单页：emoji图标选择、名称/目标/单位/步进输入、已创建列表管理 |
| `viewmodel/HomeViewModel.ets` | **修改** | taskClock 中 step 从 CustomTaskStore 获取（taskID>=8） |
| `viewmodel/TaskViewModel.ets` | **修改** | addTask 对自定义任务使用 insertData，提醒用通用文案 |
| `view/HomeComponent.ets` | **修改** | 传递自定义任务名称/图标到 TaskCard，任务编辑长按不越界 |
| `view/home/TaskCardComponent.ets` | **修改** | 自定义任务用 emoji 图标 + 自定义名称显示 |
| `view/task/TaskListComponent.ets` | **修改** | taskID=0 的入口项导航至 CustomTaskCreatePage |
| `view/task/TaskEditListItem.ets` | **修改** | TaskChooseItem 检查 customName 字段 |
| `view/dialog/TaskDetailDialog.ets` | **修改** | 打卡弹窗安全查表，自定义任务使用默认背景 |
| `model/DatabaseModel.ets` | **修改** | 4 处 for 循环 TASK_NUM 硬编码改为动态 result.length |
| `model/TaskInitList.ets` | **修改** | ITaskItem 新增 customName/customIcon/customUnit 可选字段 |
| `entryability/EntryAbility.ets` | **修改** | 保存 Context 到 GlobalContext，初始化 CustomTaskCache |
| `resources/base/profile/main_pages.json` | **修改** | 注册 CustomTaskCreatePage 路由 |

### 7.5 核心代码解析

**自定义任务存储（CustomTaskStore.ets）**：

- 使用 Preferences 存储 JSON 序列化的 `CustomTaskDef[]`
- 提供 `getCustomTaskSync(taskID)` 同步查表（从内存 Map 缓存）
- 增删改操作后自动 `refreshCache()` 保持一致性

**数据库动态化（DatabaseModel.ets）**：

```typescript
// 修改前：for (let i = 0; i < 6; i++)  ← 硬编码
// 修改后：
for (let i = 0; i < result.length; i++) {  // 动态长度
  if (result[i].isOpen === true) {
    finResult.push(result[i]);
  }
}
```

**打卡兼容（HomeViewModel.ets）**：

```typescript
let step: number = 0;
if (taskID >= 1 && taskID <= 7) {
  step = TaskMapById[taskID - 1].step;
} else {
  step = getCustomTaskSync(taskID)?.step || 1;
}
```

---

## 八、运行效果与录屏

### 8.1 运行步骤

1. 在 DevEco Studio 中打开项目，连接虚拟机
2. 点击 **Run**（或按 Shift+F10）编译运行
3. 应用启动后，进入首页

### 8.2 运行效果（配合录屏视频）

| 操作步骤 | 预期效果 |
|----------|----------|
| **步骤 1**：启动应用，观察首页顶部下方 | 在进度圆形区域和「任务列表」标题之间，显示白色圆角知识卡片，展示一条健康知识标题 + 💡 图标 |
| **步骤 2**：点击知识卡片 | 卡片展开，显示完整正文内容和分类标签（蓝色圆角标签，如「人体怪象」「饮食健康」等） |
| **步骤 3**：再次点击知识卡片 | 卡片收起，恢复为仅显示标题的单行状态 |
| **步骤 4**：正常进行打卡操作 | 点击任务卡片打卡、切换日期、切换 Tab，确认原有功能不受影响 |
| **步骤 5**：第二天再打开应用 | 知识卡片自动切换为不同内容（按日期哈希轮换） |

### 8.3 界面位置示意

```
┌─────────────────────────────┐
│         健康生活             │  ← 导航栏
├─────────────────────────────┤
│    🎯 目标进度              │
│    50 %                     │  ← 原有进度 + 周历
│    日 一 二 三 四 五 六      │
├─────────────────────────────┤
│ 💡 每天一个苹果的真正好处   ▼│  ← ★ 新增知识卡片（收起）
├─────────────────────────────┤
│    任务列表                 │  ← 原有任务列表标题
│    ☑ 早起                  │
│    ☐ 喝水  100/200 ml      │
│    ☑ 每日微笑               │
│    ...                      │
├─────────────────────────────┤
│  🏠首页  🏆成就  👤我的      │  ← 底部 Tab
└─────────────────────────────┘

      ↓ 点击卡片后展开 ↓

┌─────────────────────────────┐
│ 💡 每天一个苹果的真正好处   ▲│
│ ─────────────────────────── │
│ 一颗中等苹果含4.4克膳食纤维  │
│ （占每日推荐量17%），果胶可  │
│ 降低LDL胆固醇。连皮吃抗氧化  │
│ 能力翻倍，因为果皮中槲皮素   │
│ 含量是果肉的4倍。            │
│                             │
│ [饮食健康]                   │  ← 分类标签
└─────────────────────────────┘
```

## 九、测试要点

| 测试场景 | 验证方法 | 预期结果 |
|----------|----------|----------|
| **功能一：知识卡片** | | |
| 正常显示 | 打开首页 | 进度区下方显示知识卡片 |
| 展开 | 点击卡片 | 正文 + 分类标签出现 |
| 收起 | 再次点击 | 恢复到仅标题状态 |
| 原有功能不受影响 | 执行打卡/切换日期/Tab | 所有原有操作正常 |
| **功能二：统计图表** | | |
| Tab 显示 | 打开应用看底部导航 | 底栏显示4个Tab：首页/成就/统计/我的 |
| 统计页加载 | 点击「统计」Tab | 短暂Loading后显示4个指标卡片+柱状图 |
| 柱状图渲染 | 查看柱状图 | 7根柱子，今日蓝色高亮+圆点标记 |
| 汇总卡片数据 | 查看4个指标卡片 | 数字与数据库记录一致 |
| 每日详情行 | 滚动查看详情列表 | 每条显示星期/日期/进度条/百分比/状态图标 |
| 今日行高亮 | 查看今日对应的行 | 浅蓝背景+蓝色文字高亮 |
| **功能三：分享海报** | | |
| 全部完成触发 | 打卡完所有当天任务 | 成就弹窗后自动弹出海报弹窗 |
| 海报内容展示 | 查看海报各元素 | 日期/100%大字/任务✅清单/连续天数/成就勋章 |
| 关闭按钮 | 点击「关闭」或右上角✕ | 弹窗关闭，回到主页 |
| **功能四：自定义任务** | | |
| 创建任务 | 点击创建入口→填名称/目标/单位→点创建 | 提示成功，列表中出现自定义任务 |
| 开启任务 | 点击自定义任务→编辑页→打开开关 | 首页出现自定义任务卡片(emoji+名称) |
| 打卡 | 点击自定义任务卡片→打卡 | 任务状态更新，进度条变化 |
| 持久化 | 关闭应用重新打开 | 自定义任务仍在，打卡状态不丢失 |
| 删除 | 创建页点击删除 | 任务从列表中消失 |

---

## 十、录屏视频

| 视频文件 | 演示内容 |
|----------|----------|
| `screenshots/知识卡片录屏.MP4` | 功能一：知识卡片展开/收起 + 原有打卡流程不受影响 |
| `screenshots/统计组件录屏.mp4` | 功能二：统计Tab加载、柱状图、汇总卡片、详情列表 |
| `screenshots/成就录屏.mp4` | 功能三：全部任务完成→海报弹出→任务清单/连续天数/成就展示 |
| `screenshots/自定义.mp4` | 功能四：创建自定义任务→任务列表开启→首页打卡→重启持久化 |

---

*文档完毕。*
